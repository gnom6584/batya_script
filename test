struct A
    first I4,
    second I4

fun add(left A, right A) -> A {
    var result A
    result.first = add(left.first, right.first)
    result.second = add(left.second, right.second)
    result
}

fun factorial(value I4) -> I4
    if less(value, 2)
        1
    else
        multiply(value, factorial(subtract(value, 1)))

var l A
l.first = 4
l.second = factorial(4)
r.second

var r A
r.first = 1
r.second = factorial(2)

var res = add(l)

var array = malloc(40us)

var i = 0

struct 4
    228 I4

struct S
    5 4


var z S
var hui 4

for less(i, 5) {
    var temp A
    temp.first = add(i, 12)
    temp.second = factorial(i)
    temp = add(temp, res)
    memcpy(add(array, multiply(i, 8)), &temp, 8us)
    i = add(i, 1)
}

array
